import React from "react";
import { render, fireEvent } from "@testing-library/react";

import Run from "../extra-components/Run";

describe("<Run/>", () => {
    test("When pressed, the loading state should change to true", async () => {
        expect(true).toBe(true);
    });
});