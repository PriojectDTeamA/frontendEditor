import React from "react";
import 'regenerator-runtime/runtime'

describe("<RunButton />", () => {
    test("should change the redux state of loadingScreenOn to true", async () => {
      // ???
    });
  });